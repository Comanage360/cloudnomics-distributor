{
  "name": "cloudnomics-quote-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Guided, Claude-powered Palo Alto Networks quoting console for resellers",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
